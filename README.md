# Weather App 

Python web app made with FastHTML that shows the current temperature of a city using OpenWeatherMap API.

## Installation

```bash
git clone https://github.com/Pollyy2/weather-app.git
cd weather-app
python -m venv .venv
& .venv/Scripts/Activate.ps1  # Windows PowerShell
pip install -r requirements.txt
